import javafx.fxml.Initializable;

import java.net.URL;
import java.util.ResourceBundle;

public class Controller implements Initializable {

    /*** Parent controller class for data encapsulation of all other controllers created. This allows us to refer to a single 'type'
         (polymorphism) useful for the Stack class utilized within the Mediator class.
     ***/

    @Override
    public void initialize(URL location, ResourceBundle resources) {

    }
}
